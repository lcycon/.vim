class Order < ActiveRecord::Base
  self.inheritance_column = :inher_type
  belongs_to :cart
  has_many :selections
  has_many :photos, :through => :selections

  before_save :check_status

  private
    def check_status
      if cart.paid == true
        return false
      end
    end
end
