source 'http://rubygems.org'

gem 'rails', '3.1.0'
gem 'twilio-ruby'
gem 'jquery-rails'
gem 'launchy', '>= 2.0.5', :group => :test
gem 'resque'
gem 'resque-scheduler', " ~> 2.0.0.d"
gem 'mongoid'
gem 'bson_ext'
gem 'devise'

# get rid of a monkeypatch in rails code to handle ddos, because
# was recently patched in ruby code
# more info here:i http://stackoverflow.com/questions/7624661/rake-already-initialized-constant-warning
gem 'rack', '1.3.3'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'haml-rails'
  gem 'compass'
  gem 'compass-bootstrap'
  gem 'uglifier'
end

gem 'jquery-rails'

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

group :test do
  # Pretty printed test output
  gem 'turn', :require => false
end
